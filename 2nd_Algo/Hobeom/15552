#include <stdio.h>
int main() {
    int max, a, b;
    
    scanf("%d", &max);
    
    for(int i = 0; i < max; i++){
        scanf("%d %d", &a, &b);
        printf("%d\n", a+b);
    }
    
    return 0;
}